#include <stdio.h>


/**
 * main - function to show the value of n if it's positive,negative or = 0
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char c;
	for (c = 'a'; c <= 'z'; ++c)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
